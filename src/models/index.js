const User = require("./user");
const Image = require("./image");
const Upload = require("./upload");
const Collection = require("./collection");

const models = { User, Image, Upload, Collection };

module.exports = {
  models,
};
